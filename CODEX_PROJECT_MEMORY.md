# 项目记忆

- 项目：MSPM0 四路循迹与四路电机串口控制工程。
- 工程目录：`C:\ti\mspm0_sdk_2_02_00_05\FourWayPortal_USART`。
- 工具链：TI MSPM0 SDK，工程包含 Keil 相关文件和 SysConfig 配置。
- 当前编码规则：源码、头文件、脚本和文档统一使用无 BOM 的 UTF-8 编码；不要再转回 GB2312。
- 已完成：2026-08-05 修复 `BSP/app_motor_usart.c` 中串口接收帧边界、短帧长度校验、分割数组边界和字段复制安全检查；未改变其他业务逻辑。
- 当前未处理问题：
  - P1：UART 发送无限等待；ISR 与主循环共享接收缓冲区缺少同步；循迹分支顺序和 80 ms 阻塞延时；`sprintf` 无长度限制。
  - P2：`Deal_data_real()` 未接入主流程；`V_y` 未使用；PID `error_last` 未更新；SysTick 延时硬编码和回卷计数风险；`Motor_Usart_init()` 只有声明没有实现。
- UART3 摄像头输入：UART3 使用 PA13(RX)/PA14(TX)，MFCLK 4 MHz，115200 8N1，启用 RX 中断；外部发送端 TX 应连接 PA13，双方必须共地。`BSP/bsp_camera_usart.c/.h` 已实现 128 字节环形缓冲、AA 55 帧状态机、前 7 字节累加和低 8 位校验和 `vision` 状态；UART3 ISR 仅收字节，主循环调用 `Camera_Vision_Process()` 解析。TIMG12 提供独立的 1 ms 计时，未影响原 SysTick 延时。
- 本阶段未执行 Keil/TI 编译、烧录和实车验证。
- 已完成 UART3 摄像头链路验证：摄像头经 PA13 向 MSPM0 UART3 发送数据，MSP 曾通过 UART0 的 PA10 原样输出供串口助手确认；测试回发逻辑已移除。当前 UART3 ISR 仅将原始字节写入接收缓冲，主循环负责 8 字节协议解析。
- UniFlash 串口 BSL 的镜像 8 字节对齐：修改前 `Keil/empty.map` 的 `LR_IROM1 Size` 为 `0x9BC`（余 4）；已在 `empty.c` 函数外加入 8 字节 `__attribute__((used)) const uint32_t g_uniflash_padding[2]`。本次舵机加入后旧镜像实际为 `0x1014`，重新编译后预期为 `0x1018`；必须以新生成的 `OBJ/empty.hex` 和 `.map` 为准验证，不能手改旧 hex。
- 摄像头协议处理：仅累加和校验正确帧更新 `last_rx_ms`；仅 `FLAGS & 1` 且 `x < 320` 的新序号帧更新 x、`last_valid_ms` 和 valid。无效标志、越界、重复序号、跳号、校验和错误和环形缓冲溢出均单独记录，控制可通过 `Camera_Vision_IsUsable()` 使用 250 ms 失效阈值。舵机标定、像素到 cm 换算和 PID 尚未接入。
- UART0 解析联调输出：`BSP/bsp_camera_usart.c` 中的 `CAMERA_VISION_DEBUG_ENABLE` 当前为 `1U`，`empty.c` 已恢复 `USART_Init()`、`Camera_Vision_Init()` 和主循环 `Camera_Vision_Process()`。每处理一帧会输出有效帧（seq/x/width）、无目标帧、校验和错误、坐标越界或重复序号；验证完成后设为 `0U`，避免逐帧串口打印占用主循环时间。
- 2026-08-05 摄像头到 MSPM0 实测：UART0 调试日志累计 676 帧，SEQ 全程连续且包含 255->0 正常回绕；未出现校验和错误、坐标越界或重复序号。有效帧 x 范围为 57~289，width 范围为 8~102；带时间戳的 630 帧平均间隔 77.2 ms、最大 124 ms。通信和协议解析已验证正常；`valid=0` 连续出现时应由后续控制进入安全状态，width 很小的有效候选帧暂只记录、待实车标定质量阈值。
- 按键与 LED 联调配置（2026-08-06，已实测）：K1=PA2、K2=PB19、K3=PB20、K4=PA23，均采用低电平按下、无内部上下拉；LED1=PB2、LED2=PB3，均为高电平点亮。`BSP/key.c/.h` 提供 `Key_GetEvent()`，经过 20 ms 二次确认消抖，每次按下仅返回一次 `KEY_EVENT_K1` 至 `KEY_EVENT_K4`，全松开后可再次触发。LED 映射测试逻辑已移除；电机、循迹和摄像头业务仍保持注释。
- 舵机单路测试配置（2026-08-06，待实机验证）：选用扩展板 S2=PB9，复用为 TIMA0_CCP1；SysConfig 固定仅启用 CCP1，定时器时钟 1 MHz、周期 20000 tick（50 Hz），未占用 S1=PB8、PA0 或互补输出引脚。新增 BSP/servo.c/.h，提供 Servo_Init() 与 Servo_SetAngle()，按 500 us~2500 us 映射 0°~180°；empty.c 舵机测试逻辑已移除，当前主循环用于摄像头 UART0 解析联调。Keil 工程已加入 BSP/servo.c。舵机须使用独立稳定的 5 V 供电，并与 MSP 共地。
- Git 远程连接（2026-08-06）：`origin` 使用 SSH 地址 `git@github.com:CHN-HYH/fourwayportal-26-h.git`。此前仓库级 `http.proxy`/`https.proxy` 指向未监听的 `127.0.0.1:7892`，导致 HTTPS 推送失败；现已删除这两条本地代理配置，并通过 `git ls-remote origin HEAD` 验证 SSH 访问正常。
- 视觉舵机 P 控制测试（2026-08-06）：新增 `BSP/vision_servo_test.c/.h`，每 20 ms 使用 `vision.x` 相对 `x_center=160` 的像素误差计算舵机角度，初始 `Kp=0.20`、舵机中位 90°、方向系数 1.0；视觉无效或超过 250 ms 未更新时回到 90°。方向、中心点和 Kp 均可在 `vision_servo_test.c` 顶部调整，尚未实机验证。`empty.c` 仅保留初始化和接口调用。
- OLED 适配（2026-08-06）：在 `empty.syscfg` 中加入 I2C0，使用 PA28(SDA)/PA31(SCL)、400 kHz，已通过 SDK SysConfig CLI 重新生成 `ti_msp_dl_config.c/.h`。迁移 `BSP/oled.c/.h` 与 `BSP/codetab.h`，新增 `BSP/oled_timer_test.c/.h`；测试调用已有 K1（PA2）接口，K1 按下后从 0 秒开始在 OLED 中央计时，再次按下重新计时。
- OLED 计时接口保留（2026-08-06）：移除 K1 按键测试和 `empty.c` 中的 OLED 测试调用；保留 `BSP/oled.c/.h`、字库及计时模块，公开 `Oled_Timer_Init()`、`Oled_Timer_Start()`、`Oled_Timer_Update()`，显示固定为居中的大号 `00:00`。
- I2C OLED 计时测试恢复（2026-08-06）：当前使用 PA28(SDA)/PA31(SCL) 的 I2C0 OLED；`empty.c` 初始化 `Oled_Timer_Init()`，K1（PA2）按下调用 `Oled_Timer_Start()`，主循环调用 `Oled_Timer_Update()`，显示居中的大号 `00:00`。SPI 屏幕方案暂不迁移。

- 代码可读性整理（2026-08-11）：手写源码、头文件、SysConfig、脚本和文档已统一为无 BOM 的 UTF-8。内部变量优先使用简短名称（如 `min`、`max`、`cnt`、`idx`、`s_rx`），在声明处以中文注释说明用途、单位或数据来源；不为 `i` 等上下文充分的循环变量机械添加注释。自动生成文件与对外函数符号保持不变。本次未执行 Keil 编译、烧录和实车验证。

- 2026-08-11 舵机 0 度固定测试：`empty.c` 改为调用 `Servo_Init()` 后显式执行 `Servo_SetAngle(0U)`，主循环暂不调用视觉舵机更新，适用于验证 PB9/TIMA0_CCP1 的 0 度 PWM 输出；本次未执行 Keil 编译、烧录和实机验证。

- 2026-08-11 钢珠视觉 PD 控制融合：`BSP/vision_servo_test.c` 改为仅在新有效视觉帧到达时更新，按位置误差和滤波速度计算 `Kp * error - Kd * velocity` 的角度增量；视觉无效时回到平衡基准角。当前初始参数为目标 +5 cm、基准 15 度、限幅 0~30 度、方向 -1、像素换算 0.075 cm/px、Kp=1.00、Kd=0.20；像素换算基于水管总长 25 cm、钢球直径 1 cm，因此钢球中心行程 -12~+12 cm 对应 ROI 320 px。其余参数均须实车标定。`empty.c` 已恢复调用视觉舵机初始化与更新；`CAMERA_VISION_DEBUG_ENABLE` 设为 0，避免逐帧串口输出增加控制滞后。未执行 Keil 编译、烧录和实车验证。

- 代码命名准则（2026-08-11）：控制模块的可调宏使用简短模块前缀（例如 `SV_`），模块静态状态使用 `s_`，局部变量使用简短且含义明确的名称；宏、静态变量和关键局部变量均应附简短中文注释，避免冗长英文变量名。

- 钢珠视觉定点控制（2026-08-12，待实车验证）：`BSP/vision_servo_test.c/.h` 当前使用带抗积分饱和的 PID 闭环让钢珠稳定在固定的 `+12 cm`，不再到位后切换 `-12 cm`。实测摆杆水平为 `30°`，钢珠在 +12 cm 附近通常需约 `42°`，故使用 `SV_ANG_HOLD=42°` 作为保持角。当前 PID 为 `angle=42 + Kp*error + integral - Kd*velocity`：积分仅在 `0.2~4 cm` 误差范围、速度不超过 `1.2 cm/s` 时累加，用于快速克服静摩擦；误差跨过目标时立即清积分，避免旧积分造成过冲。机械范围已按用户补充更新为 `0°~60°`，速度项 `Kd=0.65`，且减角每帧最多 `4°`、增角最多 `2°`，确保超目标时能更快反方向倾斜制动。摄像头短暂无效时保持上一角度最多 500 ms，避免单次漏检立即回 `30°` 水平造成钢珠滚远；连续失效超时仍回安全角。日志额外输出 `i` 和未量化请求角 `req` 以便调参。未执行 Keil 编译、烧录或实车验证。

- 定点精度要求（2026-08-12）：用户要求位置误差控制在 `±0.20 cm`。现由 `SV_ERR_TOL=0.20` 明确表示：进入该范围后位置 P 项和积分项不再因像素量化抖动继续推动舵机，但速度 D 项仍保留制动作用；控制日志的 `err` 继续打印实际误差，便于实测核验。用户当前源码将 `SV_POS_REF` 设为 `10.0 cm`，本次未改动该目标值。

- 舵机机械行程实测（2026-08-12）：用户确认舵机输入 `10°` 时钢珠向负方向运动，`50°` 时钢珠向正方向运动，`30°` 为摆杆水平；后续确认完整可用范围为 `0°~60°`。视觉定点控制已据此使用 `SV_ANG0=30°`、`SV_ANG_MIN=0°`、`SV_ANG_MAX=60°`、`SV_DIR=1.0`。此前 `SV_ANG0=25°` 会在接近 +12 cm 时把舵机压到实际水平角以下，导致钢珠向负方向回滚，是中途无法稳定的主要原因。

- 视觉位置实测标定（2026-08-12，待继续验证）：用户测得旧公式 `pos=(x-160)*0.075` 时，实际 `12 cm` 显示 `10.35 cm`，实际 `5.6 cm` 显示 `4.5 cm`，实际 `6.1 cm` 显示 `4.95 cm`。按这些数据作线性标定，更新 `SV_X0=152.0`、`SV_CM_PX=0.082`；对应 `pos_new=(pos_old+0.6)*1.0933`，可使上述三个点约对应 11.97、5.58、6.07 cm。未编译、烧录或实车验证。

- 通用目标位置 PID 修正（2026-08-12，待实车验证）：用户将 `SV_POS_REF` 改为 `5.0 cm` 后，日志显示钢珠在 `pos≈11 cm`、`err≈-6 cm` 时请求角仍约 `37°`，高于 `30°` 水平角，无法向负方向移动。根因是先前为 +12 cm 端点取的固定 `SV_ANG_HOLD=42°` 被错误用作所有目标位置的基准。现已移除该固定保持角，控制公式改为 `angle=SV_ANG0(30°)+Kp*error+integral-Kd*velocity`；因此目标在左侧时会直接命令低于 30° 的角度，目标在右侧时才命令高于 30°。`Kp` 调为 1.0，其他抗积分饱和、短时视觉丢失保持和 ±0.20 cm 控制死区策略保留。未执行 Keil 编译、烧录或实车验证。

- 2026-08-12 K230 逻辑迁移建议（未改控制源码）：K230 工程目录未包含 K230 端实际识别脚本，只能参考 MSP 侧坐标协议和控制逻辑。FourWay 推荐保留现有 8 字节帧的校验和、序号、超时和短时丢失保持，不退回 K230 的仅帧尾 `FA` 校验。可迁移的有效思路为：在 `BSP/vision_servo_test.c` 对有效 `vision.x` 先做一维卡尔曼滤波；把单一 `SV_CM_PX` 改为左、右两侧独立标定；以滤波后的坐标参与位置和速度计算。ESP 协议中的 `width` 可作为候选质量门限，但需先采集真实钢珠宽度分布，初始不应盲目剔除。保持当前以 cm/s 显式计算 D 项、条件积分和 500 ms 失效降级，不能原样移植 K230 的按帧差分 PID 系数和原始 CC60 中位值，因为 PWM 标尺不同。当前 `SV_DBG=1U` 会逐帧输出控制日志，实车闭环时应设为 `0U`。

- 2026-08-12 Steel 原始基线迁移（未编译、烧录或实车验证）：用户确认真正的 K230 基线工程为 `E:\HYH\项目\26电赛H题 ESP+四路循迹+轮底v2+MSPM0\Steel_ball_control`，此前 `C:\ti\...\Steel_ball_control` 不作为 K230 原始基线。当前 FourWay 的 `BSP/vision_servo_test.c/.h` 已改为与该 Steel 控制链同构：仅新有效帧更新，`vision.x` 先经一维卡尔曼滤波（`Q=1.0`、`R=0.30`），目标 cm 以左右独立的 `SV_CM_PX_L/R` 换算至像素，按 Steel 参数执行像素域位置式 PID `Kp=0.08`、`Ki=0.003`、`Kd=2.86`、积分限幅 500、像素死区约 ±2.5 px，再将 `raw*1.80` 叠加到本车实测水平 `30°`。保留本车已验证的 `0~60°` 机械限幅、增角 2°/帧、减角 4°/帧、ESP 8 字节协议校验/序号，以及连续视觉失效 500 ms 后回水平并清状态。调试入口 `empty.c` 当前以 `SV_TEST_REF=5.0 cm` 控球；后续任务按 Steel 流程调用 `Vision_Servo_Test_Reset()` 后循环调用 `Vision_Servo_Test_Update(target_cm)` 即可复用控制器。当前左右标定系数暂同为 `0.082 cm/px`，须继续实测分别调整；`SV_DBG=0U` 已关闭逐帧日志。

- 2026-08-12 实车控制日志结论：当前调试目标 `ref=+5.0 cm` 对应图像目标约 `213 px`。日志中角度由约 `19°` 增至 `37°` 时，滤波坐标从 `76 px` 降至 `33 px`，位置从 `-6.21 cm` 继续降至 `-9.76 cm`；因此实际机械/图像方向与当前 `SV_DIR=1.0` 相反，后续应验证后改为 `SV_DIR=-1.0`。日志显示积分项长期饱和为 `+500`，在目标两侧回摆时未清除，且像素域 `Kd=2.86` 造成请求角在相邻帧间剧烈翻转，实测位置约在 `-11.13~+6.22 cm`、实际下发角约在 `6°~50°` 间大幅往复，未收敛。后续修复应至少恢复误差跨零时清积分的抗饱和逻辑；方向修正后再按实车降低 D 项和/或输出比例，不能仅继续增加 P/I。

- 2026-08-12 钢珠定点首轮修正（待实车验证）：调试入口仍固定传入 `SV_TEST_REF=+5.0 cm`，不是往返控制。根据实测日志已将 `SV_DIR` 改为 `-1.0`；当滤波后的像素误差跨过目标且超出死区时清零积分，再重新累计；为减轻相邻视觉帧跳变导致的摆杆翻转，将像素域 `SV_PID_KD` 从 `2.86` 降至 `0.80`，`SV_PID_ANG_SCALE` 从 `1.80` 降至 `1.00`。`Kp=0.08`、`Ki=0.003`、积分限幅 500、±2.5 px 死区、0~60 度限幅和视觉层 2/4 度每帧限速保持不变。实车应先确认目标位于左侧时角度会升高、目标位于右侧时角度会降低；若符号仍反向，仅调整 `SV_DIR`，不要同时翻转 PID 参数。

- 2026-08-12 PB8 舵机无控制定位：用户在 `empty.syscfg` 将舵机改为 PB8 后，SysConfig 正确生成 `TIMA0_CCP0`、`DL_TIMER_CC_0_INDEX`、`PINCM25`，但 `BSP/servo.c` 仍向旧的 `DL_TIMER_CC_1_INDEX` 写比较值，导致 PB8 保持初始值且没有控制 PWM。现已将驱动写入通道改为 `DL_TIMER_CC_0_INDEX`，并将头文件注释更新为 CCP0。PB8 无其他源码复用冲突；必须重新编译并烧录新镜像后验证 PB8 的约 50 Hz、500~2500 us 脉冲。

- 2026-08-12 像素域控制切换（待实车验证）：用户已回退视觉控制代码，当前仅将旧控制器的坐标单位由 cm 改为 px，不调整方向、舵机、协议、限速或控制结构。固定目标改为 `SV_X_REF=213 px`，即按既有标定 `x=152+5/0.082` 对应原 +5 cm 位置；位置误差、速度、积分窗口和调试日志均改为 px/px/s。为保持回退代码的物理控制力度，参数按 `0.082 cm/px` 等效换算：`Kp=0.082 度/px`、`Ki=0.0984 度/(px*s)`、`Kd=0.0533 度/(px/s)`、积分误差窗口 `48.8 px`、速度门限 `14.64 px/s`。后续调参直接使用像素量，不再依赖 cm 换算。

- 2026-08-12 像素域 PID 日志分析：目标 `213 px` 时，钢珠在 `x=190~200 px` 附近静止约 3.9 s，积分累计到上限 `+8°` 后突然运动；随后速度最高约 `176 px/s`，坐标冲到 `302 px`，正向过冲约 `89 px`，再反向摆到约 `76 px`。现有逻辑在速度超过积分门限后只停止继续累加，不会释放已经累计的积分，因此静摩擦释放后的积分冲击是主要问题。调参顺序应为：先让速度超门限时清零或快速泄放积分并把 `SV_I_MAX` 降至约 `2~3°`；再以 `Ki=0` 调 `Kp`，建议从 `0.04~0.06 度/px` 起步；出现往返后逐步增大 `Kd`，建议先试 `0.06~0.08 度/(px/s)`；必要时把 `SV_STEP_DN` 从 `4°/帧` 提高到 `6~8°/帧` 提前制动。若低速静止误差仍无法克服，应增加独立的小幅静摩擦补偿，不应继续把积分上限调大。

- 2026-08-12 改为位置式 PID（待实车验证）：`BSP/vision_servo_test.c/.h` 已删除速度计算、速度低通和按速度决定积分等逻辑，改为每个新有效像素帧执行 `err=SV_X_REF-x`、`integral+=err`、`raw=Kp*err+Ki*integral+Kd*(err-last_err)`，再按 `SV_ANG0 + SV_DIR*raw*SV_OUT_SCALE` 输出舵机角度。当前像素域参数为 `Kp=0.08`、`Ki=0.003`、`Kd=2.86`、积分限幅 500、输出缩放 1.80；保留 ±2.5 px 死区、0~60 度角度限幅、2/4 度每帧限速及 500 ms 视觉失效保护。调参只需围绕 `Kp/Ki/Kd` 三项进行。

- 2026-08-12 位置式 PID 调参入口整理（待实车验证）：`vision_servo_test.c` 将原始公式集中到 `pid_calc()`，头文件把 `SV_KP/SV_KI/SV_KD` 标为唯一调参项；积分、输出限幅和舵机换算作为固定保护参数保留。当前未执行 Keil 编译、烧录或实车验证。

- 2026-08-12 提前制动修改（待实车验证）：根据用户确认，保留 `SV_DIR=1.0`，此前日志中的角度增大后坐标减小属于钢珠过冲后的运动轨迹，不能据此判定方向反了。`vision_servo_test.c` 新增相邻帧坐标变化 `dx`；当距离目标不超过 `SV_BRAKE_START_PX=24 px`（约目标前 2 cm）且钢珠仍向目标运动时，给 PID 原始输出叠加反向 `SV_BRAKE_RAW=2.0`，约对应 3.6 度反向制动。反向运动或离开制动区后自动取消制动，位置式 PID 和原有舵机限速继续保留。日志新增 `dx` 和 `brake` 字段，便于确认制动是否触发。当前未执行 Keil 编译、烧录或实车验证。

- 2026-08-12 提前制动实测与第二轮调参（待实车验证）：新日志共 474 个控制帧，坐标范围 `28~288 px`，积分有 384 帧饱和为 `+500`。原提前制动在 `x=190`、`err=23 px` 时触发，一帧把请求角从约 `31°` 拉到约 `28°`，随后钢珠长期停在 `x=190`；制动过早过强，而普通 PID 的 `31°` 又不足以克服静摩擦。现将参数调整为 `Kp=0.035`、`Ki=0.0005`、`Kd=0.040`、积分累计限幅 `300`、PID 原始输出限幅 `1.80`；远端舵机偏角最多约 `3.2°`，目标附近以更强 P 项维持推力，积分只作小幅静摩擦补偿，D 项按帧差分提前收力。提前制动区由 `24 px` 收窄到 `16 px`（约 1.3 cm），反向制动由约 `3.6°` 降为约 `0.6°`。静态核算：`x=190` 静止时请求约 `31.7°`，每帧 4 px 缓慢接近且进入制动区时约 `30.3°`，每帧 18 px 快速接近时约 `28.8°`。当前未执行 Keil 编译、烧录或实车验证。

- 2026-08-12 位置式 PID 旧日志分析（方向结论已过期）：当时参数为 `Kp=0.007`、`Ki=0.001`、`Kd=0`，日志中坐标在 `14~294 px` 间大幅摆动、积分长期饱和。仅凭过冲轨迹曾误判 `SV_DIR=1.0` 方向相反；用户随后确认角度增大确实让钢珠从负侧向正侧移动，因此当前应保留 `SV_DIR=1.0`。该日志仍可用于说明旧参数推力不足、积分依赖过重和整数角度量化问题，但不能用于判断方向。

- 2026-08-12 第二轮参数实测日志结论（未改控制源码）：日志共 838 帧、约 96.4 s，坐标范围 `10~254 px`，12 次穿越 `x=213`；有 136 帧进入目标 ±10 px、260 帧进入 ±20 px，较上一轮能更频繁接近目标。积分有 727 帧饱和为 `+300`，PID 正向输出有 434 帧达到 `1.80` 上限。典型稳定点包括 `x=208` 保持约 4.85 s（误差 5 px、角度约 30~31°），但也会在 `x=120`、角度 `33°` 保持约 13.7 s，说明最大正向倾角仍可能无法克服静摩擦；被拨动后又会在约 `x=236~254` 和 `x=120~146` 间回摆。当前主要矛盾不是继续统一增减 `Kp`，而是静止起步需要短时更大倾角、运动后需要立即撤掉起步补偿并按速度制动；同时整数角度输出限制了目标附近的精细调节。下一步宜增加“静止大误差起步补偿”和更高分辨率 PWM 输出，PID 参数只作小幅修正。

- 2026-08-12 移除额外提前制动（待实车验证）：按用户要求，从 `vision_servo_test.c/.h` 删除 `SV_BRAKE_START_PX`、`SV_BRAKE_RAW`、相邻帧坐标 `dx`、运动方向判断和 `brake` 日志字段。当前控制量完全由位置式 PID 公式 `Kp*err + Ki*integral + Kd*(err-last_err)` 产生，参数保持 `Kp=0.035`、`Ki=0.0005`、`Kd=0.040`、积分限幅 300、PID 输出限幅 1.80；仍保留机械角度限幅、舵机每帧限速、像素死区和视觉失效保护。当前未执行 Keil 编译、烧录或实车验证。

- 2026-08-12 纯 PID 输出范围放宽（待实车验证）：四段人工拨球日志分别覆盖 `x=18~242`、`224~304`、`80~244`、`14~246`，实际舵机始终仅为 `28~33°`。根因是 `SV_OUT_MAX=1.80` 与 `SV_OUT_SCALE=1.80` 将 PID 人工限制在基准角 `30±3.24°`；例如 `x=18`、误差 195 px 时仅 P 项已为 6.825，但日志 `raw` 被截为 1.80。现保持纯位置式 PID 和 `Kp=0.035`、`Ki=0.0005`、`Kd=0.040` 不变，仅将 `SV_OUT_MAX` 放宽到 `8.00`，理论控制范围为约 `15.6~44.4°`，最终仍受 `0~60°` 机械限幅和每帧增减角限速保护。静态估算：`x=18` 静止请求约 `42.6°`，`x=82` 约 `39.0°`，`x=130` 约 `35.4°`，`x=304` 且负积分饱和时约 `24.6°`。当前未执行 Keil 编译、烧录或实车验证。

- 2026-08-12 纯 PID 第三轮日志结论（未改控制源码）：最新日志共解析到 1907 个控制帧，三段连续测试均曾到达 `|err|=3 px`，并多次到达 `|err|=5 px`。重复相同 `err` 仅说明钢珠静止，不代表 PID 停止；代码只有在 `|err|<=2.5 px` 时直接返回且不打印日志。日志中的全部 `x` 都是偶数、目标 213 为奇数，所以打印出的误差均为奇数；若进入 `x=212/214` 对应的 `err=±1` 死区，日志会暂时消失。例如 `x=228/err=-15` 时积分仍由 `+176` 持续降到 `-229`，请求角由约 `34.2°` 降到 `33.6°`，但整数角度始终量化为 `34°`；`x=218/err=-5` 的一段日志中积分最终饱和到 `-800`、执行角约 `33°`，钢珠仍未起步。误差不变时 D 项为零，因此继续增大 `Kd` 不能克服静摩擦。下一轮应先仅将 `Ki` 从 `0.001` 提到 `0.0015`，保持 `Kp=0.035`、`Kd=0.061` 和积分限幅 800，验证静止大误差时能否更快起步；若起步后过冲明显增大，再把 `Kd` 小幅提高到 `0.070~0.080`。若积分再次饱和而钢珠仍不动，参数层面可再试 `Ki=0.0018~0.002`，但根治方向是提高舵机输出分辨率并处理目标穿越后的旧方向积分，不能靠继续增加 D 解决。

- 2026-08-13 纯 PID 第四轮日志结论（未改控制源码）：用户实测参数为 `Kp=0.035`、`Ki=0.0008`、`Kd=0.21`、积分累计限幅 1300、PID 输出限幅 8、角度缩放 1.8。日志共解析 732 帧，分为两段测试，坐标范围分别为 `12~252 px` 和 `12~298 px`，最终分别停在 `err=-3 px` 和 `err=+5 px`；PID 能靠近目标，但到达前摆幅很大。积分有 301 帧达到 `+1300`，约占 41%；当前最大积分角度贡献为 `0.0008*1300*1.8=1.872°`，比此前 `0.001*800*1.8=1.44°` 反而大 30%，头文件中“积分项最大贡献 0.15”的注释已过期。典型帧 `x=244/err=-31/i=1185` 时，P 项约 `-1.09`、旧方向 I 项约 `+0.95`、静止时 D 项为 0，最终 `raw=-0.14`、舵机约 35°，说明积分饱和后的残留量抵消了反向 P 项，是越过目标后不能及时回收的直接原因。`Kd=0.21` 又会把相邻帧 `err` 跳变放大，例如 `err=-5 -> +3` 产生 `D=+1.68`，请求角升到约 40°，容易形成反向冲击；全日志有 25 帧 PID 输出饱和、58 帧请求角和实际角相差超过 1°，说明每帧限速也会削弱瞬时 D 制动。下一轮应先关闭积分按 PD 调整：建议 `Kp=0.025`、`Ki=0`、`Kd=0.10~0.12`、`OUT_MAX=5.0`；确认摆幅明显缩小后再加入 `Ki=0.0005`、`I_MAX=300~400`。不应扩大当前死区，因为现有代码进入死区后会直接保持上一角度，若带速度进入死区可能失去 D 制动。

- 2026-08-13 纯 PD 第五轮日志结论（未改控制源码）：用户将 `Kp` 降至 `0.020`、`Ki=0`、`Kd=0.10`、`I_MAX=500`、`OUT_MAX=5` 后，日志共 667 帧，坐标范围 `12~292 px`。`i` 虽可累计到 500，但由于 `Ki=0`，对输出完全没有作用。典型静摩擦停点：`x=112/err=101` 连续约 7.55 s，请求角约 `38.6~39°`；`x=194/err=19` 连续约 4.31 s，请求角约 `35.7~36°`；`x=210/err=3` 连续约 3.85 s，请求角约 `35.1°`。一旦起步，视觉帧间约 154 ms 时坐标可连续变化 14~24 px，估算速度约 90~155 px/s，并在目标附近直接从 `x=200` 越到 `x=216` 或从 `x=192` 越到 `x=236`，说明钢珠受重力加速度和惯性影响，不是匀速运动。当前 D 项使用 `Kd*(err-last_err)`，未除以实际帧间隔；由于帧间隔约 154 ms 且存在 300~923 ms 漏帧，`Kd` 实际随帧率变化，不能稳定代表速度。另一个软件问题是进入 `|err|<=SV_ERR_TOL` 时直接返回，`s_err_last` 不更新；若钢珠带速度穿过死区再离开，下一帧 D 项会相对旧误差产生额外跳变。结论：用户观察到的“静止时推不动、起步后加速过冲、反向后又因静摩擦停在大误差”成立，固定位置式 PID 无法同时解决静摩擦阈值和运动惯性。纯参数调节只能折中；稳定方案应采用按运动状态切换的控制：静止时短时起步补偿或受限积分，运动时用按 `dt` 归一化并低通的速度 D 项，误差跨零或运动方向反转时释放旧积分，并避免在有速度时直接冻结死区。当前未修改源码，未编译、烧录或实车验证。

- 2026-08-13 运动状态控制修改（待实车验证）：根据第五轮日志，`BSP/vision_servo_test.c/.h` 已由纯位置式 PID 改为“位置比例 + 静止受限积分/起步补偿 + 速度制动”。每个新有效帧按真实时间间隔计算 `dx/dt`，速度限幅到 `±250 px/s` 并以 `SV_VEL_ALPHA=0.30` 低通；低于 `15 px/s` 才视为静止，静止超过 `300 ms` 且存在误差时按误差方向加入 `SV_START_BOOST=0.80` 原始补偿，约对应 `1.44°` 舵机偏角。积分只在静止且 `2.5<|err|<=30 px` 时累计，钢珠运动时按 0.70 倍快速泄放，误差换向时清零；运动时使用 `-SV_KD*s_vel` 制动，目标死区内保留 D 项而不再直接返回。当前试验参数为 `Kp=0.020`、`Ki=0.0003`、`Kd=0.008`、`I_MAX=400`、`OUT_MAX=5`；静止 `err=101` 时理论请求约 `40.1°`，速度 `100 px/s` 时 D 项约反向 `1.44°`。日志新增 `vel`、`still`、`boost` 字段。已做静态边界检查和参数核算，未找到本机可直接调用的 Keil/EIDE 构建入口，尚未编译、烧录或实车验证。

- 2026-08-13 参考现有控制链回调（待实车验证）：运动状态版本新日志共解析 1227 帧，坐标范围 `4~300 px`，其中 793 帧持续施加 `boost`、556 帧积分达到 `±400`，最终停在 `x=224/err=-11 px`，低速持续时间约 66.9 s；持续起步补偿与按帧积分叠加后仍被整数角度量化卡在约 `33°`。现已移除 `still_ms`、固定起步 `boost`、按帧积分、raw 输出缩放和像素域速度限幅，改为与现有 cm 域控制链同构：`pos=(x-152)*0.082`、目标位置由 `x=213` 换算为约 `5.002 cm`，按真实视觉帧 `dt` 计算并以 `0.35` 低通速度；控制公式为 `angle=35 + Kp*ctl + integral - Kd*velocity`。当前 `Kp=1.0 度/cm`、`Ki=1.2 度/(cm*s)`、`Kd=0.65 度/(cm/s)`；积分只在 `0.20<|err|<=4 cm` 且 `|vel|<=1.2 cm/s` 时按时间累计，限幅 `±8°`，误差换向立即清积分；保留 `0~60°` 机械限幅、每帧增角 2°/减角 4°和视觉失效 500 ms 后回水平。已使用 Keil MDK 5.41 / ArmClang 6.22 正式重建，结果 `0 Error(s), 0 Warning(s)`，程序大小 `Code=11444, RO-data=392, RW-data=8, ZI-data=2152`，最新镜像为 `OBJ/empty.hex`；尚未烧录或实车验证。

- 2026-08-13 cm 域控制参数加速（待用户编译和实车验证）：新日志表明控制器已能依靠低速积分克服静摩擦，并可停在 `x=212/214`、`err=±0.08 cm`；但在 `err=-0.25 cm` 等静止阶段，原 `Ki=1.20` 每帧积分角度变化约 `0.046°`，建立克服静摩擦所需角度较慢。现仅将 `Ki` 提高到 `2.00 度/(cm*s)`，积分限幅仍为 `±8°`，因此只加快积分建立速度，不增加最大积分推力；到位阈值由 `0.20 cm` 收紧为 `0.15 cm`，可接受当前像素量化下的 `err=±0.08 cm`，但 `err=±0.25 cm` 仍会继续调节。`Kp=1.00`、`Kd=0.65` 和其他保护参数保持不变。用户明确要求后续编译由用户执行，Codex 默认只修改、静态检查并说明未编译。

- 2026-08-13 cm 域控制过冲调参（待用户编译和实车验证）：最新日志共 1740 帧，钢珠速度约为 `-9.41~+10.88 cm/s`，存在多次大幅往返；典型过程为钢珠在 `err=0.08 cm` 附近继续向负方向加速，最低约 `-9.41 cm/s`，随后舵机请求升高并冲至约 `err=11.7 cm`，说明主要动态问题是运动时位置推力偏大、速度制动偏弱。另有 `err=-0.57 cm`、速度接近零时积分逐渐达到 `-8°` 但钢珠仍未移动的静摩擦现象，因此不能通过继续提高 `Ki` 解决过冲。现将到位范围设为 `±0.30 cm`，`Kp` 从 `1.00` 降至 `0.75 度/cm`，`Kd` 从 `0.65` 小幅提高到 `0.75 度/(cm/s)`，`Ki=2.00` 和积分限幅 `±8°` 保持不变；先验证降低远端加速和加强运动制动能否缩小摆幅，再根据新日志单独调整。未编译、烧录或实车验证。

- 2026-08-13 cm 域积分释放修正（待用户编译和实车验证）：新日志共解析 929 帧，误差范围 `-6.64~+16.32 cm`、绝对速度峰值 `11.76 cm/s`，发生 50 次误差过零。250 帧处于 `±0.30 cm` 到位区，其中 148 帧仍保留至少 `0.5°` 积分，到位区内积分最大达到 `±8°`；例如 `err=0.08 cm、i=-6.55°` 时仍请求 `29.2°`，以及 `err=0.08 cm、i=2.19°` 时仍继续正向施力，证明旧积分会把已经到位的钢珠再次推出目标区。现保持 `Kp=0.75`、`Ki=2.00`、`Kd=0.75` 不变，将积分速度门限由 `1.20` 降为 `0.50 cm/s`；进入 `±0.30 cm` 或滤波速度超过 `0.50 cm/s` 时立即清积分，仅在目标区外、误差不超过 `4 cm` 且低速时累计，使积分只负责静止克服摩擦，运动后由位置比例和速度制动接管。后续日志应先确认运动帧和到位帧的 `i` 变为 `0.00`，再判断是否还需降低 `Kp` 或提高 `Kd`。未编译、烧录或实车验证。

- 2026-08-13 cm 域静止调节加速（待用户编译和实车验证）：积分释放修正后的日志共 682 帧、约 105.8 s，误差范围 `-3.36~+16.32 cm`、绝对速度峰值 `12.78 cm/s`，误差过零 21 次；运动时残留积分和 `±0.30 cm` 到位区残留积分均为 0，证明积分释放逻辑生效。当前调节慢主要发生在静止小误差阶段：`err=1.72 cm` 时积分从 0 建立到 `3.16°` 约需 0.9 s，`err=0.41 cm` 时建立到 `1.66°` 约需 2 s，日志中有 55 帧静止在 `err=0.41 cm`。现仅将 `Ki` 从 `2.00` 提高到 `3.00 度/(cm*s)`，理论上 `err=0.41 cm` 时达到相同起步倾角的时间由约 2 s 缩短到约 1.3 s；`Kp=0.75`、`Kd=0.75`、积分限幅 `±8°`、积分速度门限 `0.50 cm/s` 和到位范围 `±0.30 cm` 均保持不变，钢珠运动或到位后仍立即清积分。未编译、烧录或实车验证。

- 2026-08-13 外部扰动响应修正（待用户编译和实车验证）：用户手动模拟小车运动的三份日志分别为 737、592、367 帧，视觉有效帧中位间隔均约 `154 ms`，绝对速度峰值分别为 `9.92`、`10.78`、`13.37 cm/s`；钢珠一帧可移动约 `1.5~2.1 cm`，仅靠低频视觉反馈难以稳定在 `±1 cm`。日志多次出现约 `618 ms` 的有效帧间隔，旧代码因 `dt>500 ms` 将速度清零，例如钢珠由 `x=152` 跳到 `x=26` 后打印 `vel=0`，丢失了最关键的运动制动信息；同时请求角和实际角明显滞后，如请求 `40.1°` 时实际仅 `35°`。现将可用于平均速度估算的最大帧间隔放宽到 `1000 ms`，`Kd` 从 `0.75` 提高到 `1.10 度/(cm/s)`，舵机增角/减角限速由每帧 `2°/4°` 统一放宽到 `6°/6°`，`Kp=0.75`、`Ki=3.00` 和积分释放逻辑保持不变。原始整车工程在行驶任务中还使用 3 s 缓启动、前进目标偏移和按循迹转向量计算的目标前馈；其中定义的起步/停车补偿并未实际使用。FourWay 当前主循环仍未运行循迹，且循迹速度和转向量未导出给控球模块，因此手动突然推动时只能在视觉检测到位移后反馈补救；后续整车联调必须接入底盘运动前馈并限制底盘加速度。未编译、烧录或实车验证。

- 2026-08-13 自适应平衡角修改（待用户编译和实车验证）：最新日志共 762 帧，误差范围 `-1.39~+16.97 cm`。钢珠曾低速停在 `err=12.55 cm`，请求角仅约 `44.4°` 且积分始终为 0，原因是旧逻辑只允许 `|err|<=4 cm` 时积分；进入 `err=3.20 cm` 后积分虽达到 `+8°` 上限，最终请求角也只有约 `45.4°`，仍无法推动钢珠，近端低速积分饱和共 224 帧。这证明固定以 `35°` 为永久基准并限制 `±8°` 补偿无法适应机构零位或负载变化。现将 `35°` 改为上电初始估计，移除独立 `s_i`、`4 cm` 积分窗口和 `±8°` 补偿上限，新增在线学习的 `s_base`：低速 `|vel|<=0.50 cm/s` 且未进入 `±0.30 cm` 到位区时，按 `Ki*err` 修正平衡角，修正速度限为 `±6°/s`，学习范围为 `15°~55°`；钢珠运动时冻结已学习的平衡角，由 `Kp=0.75` 位置项和 `Kd=1.10` 速度项负责动态控制，到位后保留学习结果。视觉持续失效时回到已学习的 `s_base`，不再强制回到固定 `35°`。调试日志字段由 `i` 改为 `base`，用于直接观察当前学习到的平衡角。未编译、烧录或实车验证。

- 2026-08-13 平衡角校准与快速静摩擦补偿（待用户编译和实车验证）：自适应版本日志共 759 帧、约 116.7 s，`base` 在 `27.85°~54.29°` 之间变化，学习方向反转 19 次；它把克服静摩擦所需的临时倾角误当成平衡角，导致每次扰动后重新搜索。例如一次调整中 `base` 从 `27.85°` 增至 `40.90°` 约耗时 2.2 s，随后运动与回摆又耗时约 3.5 s。最终钢珠在 `err=±0.08 cm`、低速状态稳定时 `base=39.09°`，因此将当前机构平衡角校准为 `SV_ANG0=39°`，删除持续漂移的 `s_base` 学习。新增临时静摩擦推力 `s_push`：钢珠低速且未到位时按误差方向以 `10°/s` 建立，最大 `±14°`；钢珠速度超过 `0.50 cm/s`、进入 `±0.30 cm` 到位区或视觉持续失效时立即清零。动态公式改为 `angle=39 + Kp*ctl + push - Kd*vel`，其中 `Kp=0.75`、`Kd=1.10` 保持不变。日志保留 `base=39.00` 并新增 `push` 字段，用于区分固定校准角和临时静摩擦推力。未编译、烧录或实车验证。

- 2026-08-13 静摩擦推力误触发修正（待用户编译和实车验证）：固定 `base=39°` 后的两份日志分别为 331 帧/50.8 s 和 437 帧/67.1 s，最终都稳定在 `err=-0.08 cm`，说明平衡角校准有效；但第二份日志中临时推力启动 46 次、释放 47 次、累计激活 161 帧。原因是旧逻辑在滤波速度瞬时低于 `0.50 cm/s` 时立即建立推力，把回摆换向过程误判为静摩擦；典型帧为 `err=0.41 cm、push=14°、req=53.3°`，下一帧钢珠已经越过目标且 `push=0`，但普通 `6°/帧` 限速只让实际角从 `53°` 降到 `47°`，残余倾角继续加速钢珠。现新增 `SV_PUSH_WAIT_MS=500 ms`，只有连续低速且未到位达到该时间才开始建立推力；运动、到位或视觉持续失效时清空低速计时和推力。若本帧释放了非零推力，则舵机直接下发不含推力的限幅请求角，绕过普通每帧限速，避免临时推力残留。日志新增 `still` 字段：低速不足 500 ms 时 `push` 应保持 0，运动或到位后 `still/push` 应同时归零。未编译、烧录或实车验证。

- 2026-08-13 恢复简化位置式 PID（已被后续脉宽版本替代）：此前的 cm 域速度制动、自适应平衡角、静摩擦 `push/still` 等方案均已停止使用。`BSP/vision_servo_test.c/.h` 当时按原始控球结构实现：对每个新有效视觉帧进行一维卡尔曼滤波，以 `x=213 px` 为目标执行像素域位置式 PID，公式为 `u=Kp*err+Ki*sum+Kd*(err-last_err)`；参数为 `Kp=0.08`、`Ki=0.003`、`Kd=2.86`，积分与输出均限幅 `±500`，输出限幅后转为 `int16_t`，`±3 px` 死区内保持上一舵机角且不更新 PID。原 PWM 的 `0.8 CC` 输出缩放按当前 `500~2500 us/0~180°` 接口换算为 `1.8°/整数PID输出`，原 `6 CC/帧` 限速换算为 `13.5°/有效帧`；保留 PB8 舵机驱动、`0~60°` 机械范围、实测平衡角 `39°`、当前视觉协议与持续失效 500 ms 后回平衡角保护。该版本实测抽动明显，原因和替代方案见下一条记录。

- 2026-08-13 舵机改为微秒脉宽控制（已被后续纯 P 增量版本替代）：新日志共 157 帧、24.02 s，平均有效帧间隔约 `154 ms`，旧版有 107 帧请求角超出 `0~60°`、104 帧 `|raw|>=20`，实际舵机有 16 帧为 `0°`、34 帧为 `60°`，因此抽动主要来自 `Kd=2.86` 对帧差的过度放大和整数角度量化；典型过程是到位区 `err=-1.76 px` 时保持 `54°`，下一帧 `err=5.30 px` 产生 `raw=51`。该阶段为 PB8/TIMA0 的 1 MHz PWM 增加 `Servo_SetPulseUs()`，控球模块直接使用 `1 us` 分辨率的脉宽，并将 `Kd` 降至 `0.20`。微秒脉宽驱动接口仍保留，固定平衡脉宽控制已在后续版本移除。

- 2026-08-13 脉宽版静态推力修正（未继续实测，已被后续纯 P 增量版本替代）：新日志共 470 帧，出现 `x=222/err=-9 px` 连续 192 帧、`x=234/err=-21 px` 连续 87 帧，积分虽达到 `-500`，但 `Ki=0.003` 的最大贡献只有 `-30 us`，实际脉宽约 `889/870 us`，不足以克服静摩擦。当时曾把 `Kp/Ki` 提至 `0.10/0.006`，但用户指出这可能加重过冲，因此没有继续采用。

- 2026-08-13 移除固定水平脉宽并改为纯 P 增量控制（已修正为保留完整 PID）：用户已手动设置 `Kp=0.007`、`Ki=0`、`Kd=0`。为避免机构水平位置变化后固定 `39°/933 us` 影响控制，运行公式改为 `pulse_next=pulse_last+Kp*err*20 us`；`933 us` 仅作为上电初始脉宽，运行中不再作为输出基准。进入 `±3 px` 死区后保持当前脉宽，视觉失效时也保持当前脉宽，不再回固定水平位置。该阶段曾误删积分和差分代码，随后已按下一条记录恢复。

- 2026-08-13 恢复可调 Ki/Kd（已被后续标准位置式 PID 替代）：上一条中删除积分和差分属于误解用户意图；用户只是将 `Ki/Kd` 临时设为 0 进行纯 P 调试。该阶段虽恢复了完整 PID 状态，但仍错误采用 `pulse_next=pulse_last+PID*20 us`，现已按下一条记录修正。

- 2026-08-13 按原工程恢复标准位置式 PID（已被完整基线替代）：上一版错误地把位置式 PID 输出按 `pulse_last+PID` 累加，导致 308 帧日志中钢珠在 `18~304 px` 间穿越目标 14 次。现恢复为 `raw=Kp*err+Ki*sum+Kd*(err-last_err)`、`req=SV_PULSE_BASE+raw*20 us`；`s_pulse` 仅用于每帧 `20 us` 输出限速，不参与 PID 累加。保留一维卡尔曼、`±3 px` 死区、积分与输出 `±500` 限幅和 PB8 的 `1 us` 脉宽输出。`SV_PULSE_BASE=933 us` 对应原工程的 `60 CC` 零输出基准；位置式 PID 必须有该基准，机构水平变化时只重新标定此宏。考虑当前视觉帧间约 `154 ms` 且此前原值 `Kd=2.86` 会抽动，起始参数设为 `Kp=0.035`、`Ki=0.0005`、`Kd=0.120`。对旧日志离线核算，请求脉宽约 `373~1110 us`，仅 4 帧越过机械范围，限速后约 `856~1110 us`；该回放不等价于实车闭环。日志字段 `offset` 表示相对基准的脉宽偏移，不是逐帧增量。未编译、烧录或实车验证。

- 2026-08-13 位置式 PID 静摩擦积分修正（已由后续积分限幅调整继续修正）：新日志中钢珠最终停在 `x=184/err=29 px`，积分从负值累计至 `+500` 后仍连续约 6.6 s 不动；当时将 `Ki` 从 `0.0005` 恢复为原工程数值 `0.003`，最大积分脉宽贡献由 `5 us` 提高到 `30 us`。后续实测证明能够到达 `err=1 px`，但仍存在新的小误差静摩擦卡点，详见下一条。

- 2026-08-13 位置式 PID 积分上限调整（已被完整基线替代）：`Ki=0.003` 新日志共 1429 帧、约 232.6 s，曾稳定在 `x=212/err=1 px、pulse=924 us`，证明提高积分后能够克服部分静摩擦；但最终仍停在 `x=220/err=-7 px、sum=-500、pulse=898 us`，另有 `x=206/err=+7 px、sum=+500、pulse=968 us` 连续约 81.4 s 的卡点。整段穿越目标 32 次、积分饱和 772 帧，因此不继续提高 `Ki`。现仅将 `SV_I_MAX` 从 `500` 提高到 `700`，保持积分累计速度不变，将最大积分脉宽贡献从 `±30 us` 增至 `±42 us`；静止 `err=-7/+7 px` 时理论请求约为 `886/980 us`。`Kp=0.035`、`Ki=0.003`、`Kd=0.120`、标准位置式结构、`933 us` 基准和 `20 us/帧` 限速均不变。该调整可能增加积分饱和后的残留推力，下一份日志需同时检查静摩擦是否解除和过冲是否扩大。未编译、烧录或实车验证。

- 2026-08-13 位置式 PID 差分制动调整（已被完整基线替代）：`I_MAX=700` 新日志共 340 帧，坐标范围 `66.02~298.03 px`，穿越目标 9 次，积分饱和 104 帧，脉宽范围 `869~1081 us`。典型首次穿越前，`err=13.40 px、sum=700` 时仍请求 `960.5 us`，进入死区后保持该正向倾角，下一帧已越过到 `err=-10.12 px`；说明当前 `Kd=0.120` 的制动不足以抵消饱和积分和钢珠惯性。现仅将 `Kd` 提高到 `0.200`，`Kp=0.035`、`Ki=0.003`、`I_MAX=700`、标准位置式结构、`933 us` 基准和 `20 us/帧` 限速均不变，以单变量验证目标前的请求脉宽能否更早回落。旧日志中仅 5 帧请求与实发脉宽相差超过 `15 us`，因此本轮暂不放宽限速；若新日志中 `req` 已明显反向但 `pulse` 长期滞后，再单独调整限速。未编译、烧录或实车验证。

- 2026-08-13 完整恢复原始控球基线（当前方案，待用户编译和实车验证）：用户要求除摄像头串口协议外，舵机配置和钢珠控制逻辑全部与 `E:\HYH\项目\26电赛H题 ESP+四路循迹+轮底v2+MSPM0\Steel_ball_control` 一致。现已将 `empty.syscfg` 的舵机 PWM 对齐为 `PB8/TIMA0_CCP0`、总线时钟先除 8、预分频 100、定时器频率 `40 kHz`、周期 `800 CC`、通道反相、上电自动启动；`CC=20/60/100` 分别对应 `500/1500/2500 us`。已通过 SysConfig 1.21.1 使用 SDK 的 `.metadata/product.json` 重新生成 `ti_msp_dl_config.c/.h`，生成过程仅有 STOP/STANDBY 寄存器保持提示，无配置错误。`BSP/servo.c/.h` 已恢复原始 `Servo_Init()`、`Set_Servo_Angle()`、`Servo_SetPos()` 接口和舵机保护行为。`BSP/vision_servo_test.c/.h` 已按原始有效控制路径重写：一维卡尔曼 `Q=1.0/R=0.3`、位置式 PID `Kp=0.08/Ki=0.003/Kd=2.86`、积分和 PID 输出限幅 `±500`、PID 浮点输出转 `int16_t`、`CC=60+out*0.80`、`±3 px` 死区、每个有效帧最大变化 `±6 CC`、最终限幅 `20~100 CC`，每 15 个实际控制帧输出一次 `x/e/cc`；原代码注释声称但没有实现的“误差换向清积分”没有额外补入。当前 8 字节串口协议及校验、序号、有效帧解析保持不变，控制入口以 `vision.valid` 对应原 `g_valid`，再用 `vision.valid_n` 保证每个有效帧只消费一次。由于当前识别输出为 `0~319 px`，而原控制参数基于 `99~599 px`，适配层先用本车标定 `pos=(x-152)*0.082 cm` 换算物理位置，再按原控制器左右独立比例映射到 `99~599 px`；当前 `x=213` 映射为约 `455.638`，与原工程 `target_cm=5.0` 的目标 `455.600` 相差约 `0.038 px`，小于原死区。单独调试入口仍固定传入 `SV_TEST_TARGET_CM=5.0 cm`。此前 `933 us` 基准、`Kp=0.025/Kd=1.5/I_MAX=700`、微秒脉宽输出及所有静摩擦状态控制方案均已停止使用。按用户要求未编译、烧录或实车验证。

- 2026-08-13 完整基线首次运行无动作排查（待用户编译和实车验证）：用户反馈舵机不动且没有控制日志。确认原始控制日志只有真正进入 PID 的帧累计到 15 次才输出，死区、无有效帧或入口被拦截时均没有日志，无法定位链路。协议适配层此前额外检查最新 `vision.valid`，但 `Camera_Vision_Process()` 每轮会清空整个 UART3 缓冲区，若同一轮先解析有效坐标、后解析无目标帧，`valid_n` 已增长而最终 `valid=0`，会丢弃尚未消费的有效坐标；原工程的 `g_valid` 会一直保留到控制函数消费。现已删除该当前状态门槛，只以 `valid_n` 是否增长判断有无尚未消费的有效坐标，恢复原始一次性消费语义。新增上电日志 `[SERVO] init pin=PB8 timer=TIMA0_CCP0 cc=60`，并每秒输出一次 `frame/valid_n/valid/x/sum_err/overflow` 链路心跳；这些日志不参与 PID 计算。若烧录后连 init 日志都没有，应检查新镜像是否实际运行、UART0 PA10、115200 和串口助手；若 `frame=0`，检查摄像头 TX 到 PA13 及共地；若 `frame` 增长但 `valid_n=0`，检查协议 flags、坐标或校验；若 `valid_n` 增长，控制入口已经工作，再检查 PB8 PWM。未编译、烧录或示波器验证。

- 2026-08-13 中心定点调试目标：当前钢珠单独调试目标由 `+5.0 cm` 改为摆杆中心 `0.0 cm`。控制器内部目标坐标为 `CTRL_CENTER_X=360`，按当前输入标定对应摄像头原始坐标约 `x=152 px`；卡尔曼滤波、位置式 PID、死区、舵机限速和 PB8 PWM 配置均保持不变。待用户重新编译、烧录并实车验证。

- 2026-08-13 舵机与摄像头链路隔离自检（当前 `empty.c` 调试入口，待用户编译和实车验证）：暂时不初始化或运行视觉 PID。上电后直接控制 PB8/TIMA0_CCP0，依次输出 `CC=40`、`CC=80`、`CC=60`，每档保持 1 秒，分别约对应 `1000 us`、`2000 us`、`1500 us`，用于确认舵机供电、共地、接线和 PWM 是否正常；随后保持中位。舵机测试结束后初始化 UART3 摄像头接收，主循环对每个新有效目标打印原始 `x/width/seq/valid_n`，正确无目标帧打印 `no ball`，并每秒打印 `frame/valid_n/valid/sum_err/overflow` 链路状态。原视觉 PID 模块和 `0 cm` 目标配置均保留，仅从 `main` 暂时断开。

- 2026-08-13 机械水平位调整（当前 `empty.c` 调试入口，待用户实车调整）：舵机扫动自检已完成，现取消 `CC=40/80/60` 扫动及三段延时。上电后由 `Servo_Init()` 直接将 PB8/TIMA0_CCP0 固定在 `CC=60`（约 `1500 us`）中位，用户据此调整摆杆机械结构至水平；视觉 PID 继续断开，摄像头原始坐标和每秒链路状态日志继续输出。

- 2026-08-13 恢复中心闭环控制（当前 `empty.c` 入口，待用户编译和实车验证）：用户已按 `CC=60` 将机械结构调整至水平。`empty.c` 已移除舵机固定和摄像头逐帧自检逻辑，恢复 `Camera_Vision_Process()` 后调用 `Vision_Servo_Test_Update(SV_TEST_TARGET_CM)`；当前 `SV_TEST_TARGET_CM=0.0 cm`，对应摄像头原始中心约 `x=152 px`、控制域目标 `x=360`。闭环沿用当前原始基线的一维卡尔曼、位置式 PID `Kp=0.08/Ki=0.003/Kd=2.86`、`CC=60` 水平基准、`±3` 控制域像素死区、每帧 `±6 CC` 限速及 `20~100 CC` 限幅。模块保留每秒链路状态和每 15 个实际控制帧的 `x/e/cc` 日志。

- 2026-08-13 舵机动作导致摄像头重启（修正结论）：实测同一供电和舵机在完整恢复原始配置前不会导致摄像头重启，因此不能只归因于原有硬件供电能力。当前 `40 kHz/800 CC/反相` PWM 配置换算正确，`CC=20/60/100` 分别约为 `500/1500/2500 us`；软件变化才是新增触发条件。恢复前控制范围为 `500~1166 us`、单帧最大变化 `20 us`、`Kd=1.5`，当前完整基线开放为 `500~2500 us`、单帧最大变化 `6 CC=150 us`、`Kd=2.86`，闭环开始后更容易快速换向、产生大行程或触碰机械端点，导致舵机瞬时电流增加并使共享供电上的摄像头复位。电源跌落仍是重启机制，但需先通过收窄舵机机械安全范围、降低每帧变化量并观察实际 `cc` 日志定位软件触发条件，再决定是否加强供电和去耦。

- 2026-08-13 中心闭环保守收敛参数与逐帧日志（待用户编译和实车验证）：针对日志中实际 `CC=35.6~100.0` 大幅往复、正误差时 D 项仍能把舵机反向拉到水平位以下的问题，将控制域位置式 PID 从 `Kp=0.08/Ki=0.003/Kd=2.86` 调为 `Kp=0.035/Ki=0.001/Kd=0.040`；机械软件范围按用户要求限制为 `CC=40~80`（约 `1000~2000 us`），相邻有效帧最大变化从 `6 CC` 降为 `1 CC`（25 us）。首个有效帧将当前误差作为差分初值，避免 `last_err=0` 造成首次 D 冲击；死区内同步 `last_err`，避免离开死区时产生差分跳变。`Camera_Vision_Process()` 改为每次主循环最多交付一帧正确协议帧，避免 UART 打印期间积压的中间坐标被最后一帧覆盖。每个有效帧均输出 `[BALL]` 日志，包括 `valid_n/raw_x/flt_x/pos/target/err/p/i/d/out/req/cc/hold`；`raw_x` 是摄像头原始坐标，`flt_x` 是滤波后还原的摄像头坐标，`pos` 为厘米位置，`target=360` 是内部控制域的 `0 cm` 目标。

- 2026-08-13 舵机机械限位修正（当前方案，待用户标定）：`CC` 是 PWM 比较计数而不是舵机角度，当前时基下 `1 CC=25 us`。按代码中的标称映射，`CC=60±15` 对应舵机轴约 `±33.75°`，并非 `±15°`；若实际舵机脉宽行程或机械连杆比例不同，摆杆转角还可能超过 `40°`。用户实测 `45~75 CC` 已撞到底板并堵转，且检查时源码曾被外部改为更危险的 `30~70 CC`。闭环保护现暂时收紧为 `55~65 CC`（`1375~1625 us`，相对中位 `±125 us`），PID 和每帧 `1 CC` 限速不变。最终上下限必须断开 PID 后用固定 CC 逐步标定，不应通过闭环碰撞确定。

- 2026-08-13 中心目标运行日志结论（未改控制源码）：新日志共 642 个有效控制帧，所有帧的内部目标均为 `target=360`，对应当前协议 `raw_x=152 px` 和物理位置 `0 cm`，不是 5 cm。钢珠在日志中穿越 0 cm 共 8 次，处于 `±0.3 cm` 内 22 帧，并在 `pos=0.08 cm/err=-1.60`、`pos=-0.11 cm/err=2.35` 等位置触发 `hold=1`，证明目标换算与到位判断确实指向中心。最终停在 `raw_x=222/pos=5.74 cm` 时仍为 `hold=0`、`err=-109.75`，控制器正请求回中心；但 `P=-3.84`、`I=-0.50`、`D=0` 经整数截断后仅得到 `req=cc=56.8`，静态倾角不足以克服摩擦。整段位置范围为 `-8.16~+11.79 cm`、CC 范围为 `55~64.8`、UART 溢出为 0。后续纯 PID 调整应增强静止大误差时的持续输出，同时保持 `55~65 CC` 机械保护，不应修改目标值。

- 2026-08-13 重复正侧停点代码检查（未改控制源码）：第二份日志共 233 帧，目标仍全部为 `360`，钢珠穿越中心 3 次，最终从 `5.33 cm` 回落后连续 25 帧停在 `raw_x=210/pos=4.76 cm`；对应 `err=-90.93`、`P=-3.18`、`I=-0.50`、`D=0`、浮点 PID 输出 `-3.68`。当前 `pid_calc()` 将该值强制截断为 `int16_t -3`，`pid_to_cc()` 因而得到 `57.6 CC`，最终 PWM 写入又四舍五入为整数 `58 CC`；日志中的小数 CC 并非硬件实际分辨率。积分限幅 `±500` 配合 `Ki=0.001` 最多只贡献 `±0.5`，在整数截断前后经常不能改变实际 CC 档位，因此无法消除水平基准偏差或静摩擦造成的稳态误差。代码中没有第二个 5 cm 目标，也没有其他业务模块写 TIMA0_CCP0。重复停在正侧约 5 cm 更符合 `CC=60` 控制基准与实际动态平衡 CC 存在偏差、再叠加输出量化和积分过弱；应先断开 PID，用固定整数 CC 验证 `58/59/60` 在钢珠位于中心时的静态运动方向，再决定修正 `PWM_CC_CENTER` 或增强积分，不能把 `INPUT_CENTER_X` 改到 210 来掩盖机械偏置。

- 2026-08-13 中心死区与 PID 量化修正（待用户编译和实车验证）：此前钢珠进入 `±3` 控制域像素死区时直接保持 `s_cc_last`，若舵机此前仍处于倾斜 CC，手动将钢珠放到 0 cm 后摆杆仍会推动钢珠离开中心；旧积分也未清零。现进入死区后立即清空积分和 PID 输出、同步当前误差到 `last_err`，并按每帧 `1 CC` 限速让舵机回到水平基准 `CC=60`，不再保持旧倾角。`pid_calc()` 的输出由 `int16_t` 改为 `float`，取消乘 `0.8` 前的整数截断；PWM 硬件仍按整数 CC 写入，但请求跨档阈值不再额外损失。普通控制与死区回中统一通过 `set_cc()` 执行 `55~65 CC` 机械限幅、每帧限速和 PWM 写入。逐帧日志新增 `pwm` 字段，表示最终写入定时器的整数 CC，`req/cc` 改为保留两位小数，便于区分浮点请求、限速状态和硬件实际档位。参数 `Kp=0.035/Ki=0.001/Kd=0.040` 暂时不变。

- 2026-08-13 物理中心坐标校准（待用户编译和实车验证）：用户将钢珠放在摆杆物理正中时日志显示 `err=-9.41`。当前正侧控制域比例约为 `1.568 控制像素/输入像素`，该误差对应输入坐标偏差约 `6 px`，因此将 `INPUT_CENTER_X` 从 `152.0` 修正为 `158.0`。目标仍为 `0 cm`、内部 `target=360`；上电提示同步改为 `camera_center_x=158`。校准后钢珠位于物理中心时，预期 `raw_x≈158`、`pos≈0`、`err≈0`。

- 2026-08-13 中心振荡收敛调参（已由后续限速与积分调整继续修正）：中心校准后的日志共 662 个有效帧、约 70.36 s，位置范围 `-7.64~+7.18 cm`，穿越 0 cm 共 22 次，最终能够稳定在 `raw_x=158/pos=0/pwm=60`，UART 溢出为 0。峰值包络虽逐步衰减，但高速穿越中心时旧死区逻辑会立即清零 D 项并回水平，撤掉当帧制动；误差跨零后旧积分也会短暂保持原方向。现保持 `Kp=0.035` 以保留克服静摩擦的能力，将 `Ki` 从 `0.001` 降为 `0.0008`，将 `Kd` 从 `0.040` 提高到 `0.080`。误差跨零时清空旧积分；只有 `|err|<3` 且 `|err-last_err|<4` 控制像素/帧时才判定稳定到位并回 `CC=60`，高速穿越死区时继续执行 PID，使 D 项保留反向制动。逐帧日志新增 `derr` 字段记录该帧误差变化。调参复核期间源码的机械限幅被外部同步为当前 `50~70 CC`，本次未覆盖该变化；每帧 `1 CC` 限速保持不变，实车必须确认新范围不会碰撞机构。

- 2026-08-13 中心控制限速与静摩擦修正（当前方案，待用户编译和实车验证）：最新日志共 1046 个有效帧，位置范围 `-12.29~+8.19 cm`，穿越中心 28 次，仅 2 帧真正进入稳定状态；`req` 与实际 `cc` 最大相差 `7.53 CC`。典型过零帧中控制器已请求 `req=57.89/62.98` 反向制动，但每帧 `1 CC` 限速使实际值仅到 `cc=59.08/61.06`，因此不继续单纯增加 `Kd`，而将限速放宽为每有效帧 `2 CC`。日志末尾连续停在 `raw_x=150/pos=-0.66 cm/err=13.70`，旧 `Ki=0.0008` 即使积分饱和也只有 `I=0.40`、最终 `PWM=61`，不足以可靠克服静摩擦；现恢复 `Ki=0.003`，但仅在 `|derr|<4` 控制像素/帧时累计积分，运动过程中不继续堆积，误差跨零仍清积分。另有一次有效视觉间隔约 `3466 ms`，恢复帧把位置跳变计算成 `derr=126.87`；现当有效帧间隔超过 `300 ms` 时重置卡尔曼、积分和差分历史，使恢复首帧 `derr=0`。`Kp=0.035`、`Kd=0.080`、目标 `0 cm`、输入中心 `158 px` 均不变。源码机械范围仍为用户当前设置的 `50~70 CC`，未擅自覆盖，但该范围比此前碰撞过的范围更宽，实车测试前必须确认不会撞底板。未编译、烧录或实车验证。

- 2026-08-13 中心静摩擦积分上限放宽（当前方案，待用户编译和实车验证）：负侧日志共 131 个有效帧，钢珠始终停在 `raw_x=132/pos=-2.13 cm/err=44.52`，全部帧均为 `P=1.56/I=1.50/D=0`、`req=cc=62.45`、实际 `PWM=62`。正侧后续日志同样始终停在 `raw_x=176/pos=+1.48 cm/err=-28.22`，且 `I=-1.50`、`req=cc=58.01`、实际 `PWM=58` 连续不变。两侧的 `I=±1.50` 都正好等于 `Ki 0.003 × 原积分上限 ±500`，证明是对称的积分饱和，既不是 `50~70 CC` 限幅，也不是每帧 `2 CC` 限速。现已将积分累计上限从 `±500` 放宽为 `±1000`，并提取为 `PID_I_LIMIT`；最大积分项由 `±1.50` 增至 `±3.00`。相同静止状态下，负侧理论请求将逐步到约 `63.65 CC`、整数 PWM 约为 `64`；正侧理论请求将逐步到约 `56.81 CC`、整数 PWM 约为 `57`。低速积分门限、过零清积分、`Kp/Ki/Kd`、目标和机械 CC 范围均不变。积分数值上限增大表示限制减弱，并非把上限数值减小；后者只会让输出更弱。两份日志均为旧上限固件产生，需重新编译烧录后观察 `i` 能否越过 `±1.50`。未编译、烧录或实车验证。

- 2026-08-13 中心到位区调整（当前方案，待用户编译和实车验证）：用户已自行将控制参数改为 `Kp=0.034`、`Ki=0.003`、`Kd=0.31`、积分累计上限 `±2000`，这些实车参数予以保留。新日志中钢珠在 `err=10.27` 静止时积分项持续累加至约 `I=5.32`，开始移动后旧逻辑只停止继续积分，不会卸掉已经积累的推力；到 `err=4.22` 时仍有 `I=5.36`、请求 `63.58 CC`，下一帧便穿过目标。按用户明确要求，现将到位条件改为仅判断 `|err|<5`，不再额外要求 `|derr|<4`；进入到位区立即清空积分和差分输出，并请求舵机回 `CC=60`。到位范围是控制域像素，中心附近约对应物理 `±0.24 cm`。未编译、烧录或实车验证。

- 2026-08-13 `+5 cm` 到 `-5 cm` 两阶段调试流程（当前方案，待用户编译和实车验证）：`empty.c` 上电后先把目标设为 `+5.0 cm`；控制模块对每个新有效帧返回无新帧、移动中或到位三种结果。钢珠连续 `10` 个有效帧满足当前 `|err|<5` 的到位条件后，主流程只切换一次到 `-5.0 cm`；在 `-5.0 cm` 再连续到位 `10` 帧后永久保持该目标，不再往返。目标切换时控制模块清空旧积分，并将新误差同步为差分历史，使切换首帧 `derr=0`，避免 `+5` 到 `-5` 的设定值跃变产生 D 冲击。串口会依次输出 `[CONTROL] target=5.0cm`、`[CONTROL] target=-5.0cm` 和 `[CONTROL] target=-5.0cm stable`。用户调参过程中源码当前实际参数已更新为 `Kp=0.028`、`Ki=0.003`、`Kd=0.28`、积分累计上限 `±1800`，流程修改未覆盖这些参数。未编译、烧录或实车验证。

- 2026-08-13 两阶段 5 秒快速控制（当前方案，待用户编译和实车验证）：旧实测日志从 `22:05:35.862` 到 `22:07:46.274` 共约 `130.4 s`、821 个有效帧，平均仅 `6.29 Hz`；`+5 cm` 阶段约 `5.39 s`，`-5 cm` 阶段约 `124.9 s`。目标切到 `-5 cm` 后，PWM 前三帧已从 `60` 降到 `54`，钢珠约 `1.4 s` 从 `+5.25 cm` 到 `-2.45 cm`，说明大行程初始响应足够；主要耗时是之后在多个位置静止并等待积分逐帧爬升，例如 `-3.61 cm` 连续静止约 `5.4 s`、`-3.41 cm` 连续静止约 `8.3 s`。现将阶段到位确认从连续 `10` 帧改为 `1` 帧，消除每阶段约 `1.5 s` 的确认等待；在 `|derr|<1` 且仍未进入到位区时加入实测静摩擦补偿，正方向请求不低于 `CC=65`，负方向请求不高于 `CC=54`，一旦钢珠开始明显运动便恢复原 PID 输出和 D 项制动。新增从控制初始化开始的毫秒计时：目标切换打印 `elapsed`，完成打印 `stable elapsed`，超过 `5000 ms` 未完成则打印一次 `timeout elapsed/stage`，但不会强制停止控制。用户当前源码参数为 `Kp=0.028`、`Ki=0.005`、`Kd=0.45`、积分累计上限 `±1300`、到位区 `|err|<10`，本次未覆盖。快速补偿仍受 `50~70 CC` 机械限位和每有效帧 `2 CC` 变化限制。该方法消除了日志已证明的软件等待，但 5 秒能否满足仍须实车验证，不能由离线日志保证。未编译、烧录或实车验证。

- 2026-08-14 位置增益调度与有限启动补偿（当前方案，待用户编译和实车验证）：用户确认摆杆转轴位于物理 `+12 cm`，同样的 PID 输出在负半轴作用明显更强。`BSP/vision_servo_test.c` 现以 `+5 cm`（距转轴 `7 cm`）为参考，按当前位置计算 `gain=sqrt(7/(12-pos_cm))` 并限幅到 `0.55~1.10`；典型值约为 `+5 cm=1.00`、`0 cm=0.76`、`-5 cm=0.64`。仅用该系数缩放 P、D 和静摩擦启动脉冲相对 `CC=60` 的幅度，保留用户当前 `Kp=0.028`、`Ki=0.005`、`Kd=0.45`、积分上限 `±1300`、机械限位 `50~70 CC`、每有效帧 `2 CC` 限速及两阶段目标流程。原先低速时持续强制 `CC=65/54` 的补偿改为最多 `3` 个有效帧的启动脉冲，之后冷却 `4` 个有效帧才可再次触发；检测到钢珠运动、目标切换、视觉历史重建或进入到位区时会撤销或重置补偿。逐帧 `[BALL]` 日志新增 `gain` 和 `kick`，其中 `kick=1` 表示该帧启用了启动补偿。该换算主要修正摆杆快速转动的瞬态差异，不代表静态重力关系的精确模型，必须根据实车正负半轴日志继续校准。当前 PATH 中未找到 Keil、ARMClang 或 TI Arm Clang，未编译、烧录或实车验证。
- 2026-08-14 非零目标保持逻辑修正与首阶段 20 帧确认（当前方案，待用户实车验证）：四份新日志显示 `+5 cm` 首次到位约需 `3.30~5.45 s`，旧流程只要一个 `hold=1` 帧就立即切换到 `-5 cm`。进入 `-5 cm` 到位区后，旧控制器又清空积分并强制舵机回 `CC=60`，使钢珠从约 `-5 cm` 退回 `-3.28~-4.59 cm`；随后积分反复恢复并最终饱和到 `I=-6.50`，这是非零目标不能稳定保持的主要原因，不是单纯 P/D 过大。现将阶段确认拆分为 `SV_TEST_FIRST_STABLE_FRAMES=20` 和 `SV_TEST_SECOND_STABLE_FRAMES=1`，只有 `+5 cm` 需要连续保持 20 个有效帧后才切换。`Vision_Servo_Test_Update()` 对 `0 cm` 目标仍在到位区清积分并回水平 `CC=60`，但对 `+5/-5 cm` 非零目标会在到位区继续运行 PID 并保留所需倾角；误差跨零仍先清旧积分。根据日志只将 `Ki` 从 `0.005` 降为 `0.003`，保留 `Kp=0.028`、`Kd=0.45` 和位置增益调度，因为 D 项在大行程中主要提供反向制动，直接降低会增大过冲。`Keil/empty.map` 在本次工作期间被外部编译更新，未回退或修改。未由 Codex 编译、烧录或实车验证。
- 2026-08-14 非零目标到位角度锁定（当前代码改动，待用户实车验证）：用户指出到位后仍执行 PID 积分会再次推动钢珠。`BSP/vision_servo_test.c` 现对 `+5/-5 cm` 非零目标采用角度锁定：第一次进入 `|err|<DEADBAND_PX` 时保存当前实际 `s_cc_last`，后续直接保持该 CC，不再调用 `pid_calc()`，并将积分、P/D 和输出状态清零，防止积分继续累加；钢珠被扰动离开误差区后才清除锁定并恢复闭环。`0 cm` 目标仍沿用进入误差区回水平 `CC=60` 的逻辑。当前工作树中的用户参数以源码为准（目前 `Ki=0.005`、首阶段确认帧数为 `10`），本次未覆盖。未由 Codex 编译、烧录或实车验证。
- 2026-08-14 内层低速锁角与误差回差（当前代码改动，待用户实车验证）：用户要求将非零目标的锁角条件收紧为动态内层。`BSP/vision_servo_test.c` 新增 `HOLD_ENTER_ERR_PX=8` 和 `HOLD_MAX_DERR_PX=3`；只有 `|err|<8 px` 且相邻有效帧误差变化 `<3 px/帧` 时才保存当前 CC 并冻结 PID。`DEADBAND_PX=10` 作为外层退出阈值，锁定后误差达到 `10 px` 或速度超过内层阈值便恢复 PID，形成 8/10 px 回差，降低边界抖动。零点目标也使用同样的低速进入条件，快速穿越目标时保留 PID 和 D 项制动。当前用户在工作树中自行调整的 PID 参数和阶段确认帧数均未覆盖；本次未编译、烧录或实车验证。
- 2026-08-14 去除锁角速度门限（当前代码改动，待用户实车验证）：用户确认锁角判断中的 `derr` 与静止判断作用重复。现将 `hold_ready` 简化为仅判断 `|err|<HOLD_ENTER_ERR_PX`（当前为 `8 px`）；`DEADBAND_PX=10` 仍作为锁角退出阈值。PID 的 D 项仍保留，静摩擦补偿的 `STATIC_DERR_PX` 判断也保留，因此只去除了锁角入口的速度条件。`SV_TEST_SECOND_STABLE_FRAMES=1` 未修改。未编译、烧录或实车验证。
- 2026-08-14 位置增益下限小幅提高（当前代码改动，待用户实车验证）：按用户要求仅做最小修改，将 `GAIN_SCALE_MIN` 从 `0.55` 提高到 `0.70`，位置换算公式、参考位置和增益上限均不变。`-5 cm` 处原始计算值约 `0.64`，现会被下限提升到 `0.70`；`0 cm` 约 `0.76`、`+5 cm` 为 `1.00`，基本不受影响。当前用户自行调整的 PID 参数、锁角和阶段流程均未覆盖。未编译、烧录或实车验证。
- 2026-08-14 舵机输出平滑与锁角回差修正（当前代码改动，待用户实车验证）：针对舵机控制一抽一抽的问题，将 `RATE_LIMIT` 从每有效帧 `2 CC` 降为 `1 CC`，使 PWM 请求每帧最多变化约 `25 us`。同时修正此前实际未形成的锁角回差：非零目标在 `|err|<8 px` 进入锁角后，误差处于 `8~10 px` 时继续保持保存的 CC，只有达到外层 `|err|>=10 px` 才解除并恢复 PID，避免在 8 px 边界反复切换。为避免限速降低后 3 帧启动补偿尚未达到静摩擦角度便结束，将 `STATIC_PULSE_FRAMES` 从 `3` 延长到 `5`；检测到钢珠运动后仍会立即撤销。当前用户自行调整的 PID、gain、机械限位和阶段参数均未覆盖。未编译、烧录或实车验证。
- 2026-08-14 视觉舵机参数集中管理：`BSP/vision_servo_test.c` 中的 PID、坐标标定、舵机限位、锁角阈值、静摩擦补偿、限速、卡尔曼滤波和位置增益宏已原值迁移到 `BSP/vision_servo_test.h`，并按功能分组补充中文注释。后续实车调参统一修改该头文件；本次未改变控制流程和用户当前参数。`git diff --check` 通过，未执行 Keil 编译、烧录或实车验证。
- 2026-08-14 K4 启动 `0 -> 5 -> -5 cm` 流程（待用户编译和实车验证）：`empty.c` 上电后持续以 `0 cm` 为闭环目标并等待 K4，不再自动运行。K4 按下后重新从原点开始，原点、`5 cm` 和 `-5 cm` 每个阶段连续 6 个有效帧满足 `|err|<5 px` 后停止 PID 并立即切换目标，最后保持 `-5 cm`；运行中再次按 K4 会回原点并重启整段流程。主流程已删除重复的稳定帧计数，动作计时从 K4 按下时开始。本次未擅自修改坐标标定和 PID 参数。
- 2026-08-14 删除舵机锁角逻辑（当前方案，待用户编译和实车验证）：`BSP/vision_servo_test.c/.h` 已移除保存舵机 CC 的状态、锁角进入阈值、误差回差、零点强制回中位以及锁角期间冻结和清零 PID 的全部分支。现在每个新有效视觉帧始终执行位置式 PID 并更新舵机；`DEADBAND_PX=8` 仅用于到位判定和静摩擦补偿。控制结果改为 `VISION_SERVO_REACHED`，逐帧日志字段由 `hold` 改为 `reached`，两阶段到位帧统计和 K4 回原点入口继续保留。用户当前 PID、位置增益、舵机限位和限速参数未修改。
- 2026-08-14 连续到位后停止 PID（当前方案，待用户编译和实车验证）：针对日志中 `err=0` 仍因积分项持续增加角度的问题，`BSP/vision_servo_test.h` 使用短宏 `STOP_ERR_PX=5` 和 `STOP_FRAMES=6`。控制器连续 6 个有效帧满足 `|err|<5 px` 后清零 PID 内部项、静摩擦补偿和输出状态，保持当前实际舵机 CC 不变；误差离开 5 px 后清除停止状态并恢复 PID，恢复首帧同步误差历史以避免 D 冲击。`VISION_SERVO_REACHED` 只在真正停止后返回，主流程收到后立即切换目标，不再进行第二次连续帧计数。
- 2026-08-14 大误差积分限制与快速反向制动（当前方案，待用户编译和实车验证）：`+5 -> -5 cm` 日志显示钢珠启动前积分已达到 `i=-5.58`，D 项虽提前给出反向制动请求，但普通 `1 CC/帧` 限速使实际舵机输出明显滞后。现新增 `INTEGRAL_ERR_PX=80`，仅在 `|err|<80 px` 且单帧误差变化满足原条件时累计积分，大误差阶段直接清零积分；新增 `BRAKE_RATE_LIMIT=2 CC/帧`，当舵机输出变化方向与误差变化方向一致、即正在抑制钢珠运动时使用快速限速，其他情况仍保持 `RATE_LIMIT=1 CC/帧`。用户当前 `STOP_ERR_PX=8`、`GAIN_REF_ARM_CM=5.5` 及 PID 参数均未覆盖。`git diff --check` 通过，未执行 Keil 编译、烧录或实车验证。
- 2026-08-14 正负半轴位置比例重新标定（当前方案，待用户编译和实车验证）：用户实测实际 `+5 cm` 时旧日志显示 `pos=+4.75`，实际 `-5 cm` 时显示 `pos=-4.90`。为保留已确认的中心 `INPUT_CENTER_X=158`，将原统一 `INPUT_CM_PER_PX=0.082` 拆为正半轴 `INPUT_CM_PX_POS=0.0863` 和负半轴 `INPUT_CM_PX_NEG=0.0837`，输入控制换算和调试日志反算均按位置符号选择比例。按旧标定点回算，新位置分别约为 `+4.999 cm` 和 `-5.002 cm`，同时 `raw_x=158` 仍对应 `0 cm`。用户期间手动调整的 `PID_KI=0.0048`、`PID_KD=0.4`、`INTEGRAL_ERR_PX=110` 等参数未覆盖。`git diff --check` 通过，未执行 Keil 编译、烧录或实车验证。
- 2026-08-14 积分平滑介入（当前方案，待用户编译和实车验证）：反向 `+5 -> -5 cm` 日志显示，旧逻辑在 `|err|<110 px` 后还需等待 `|derr|<4 px/帧` 才从零开始积分，钢珠被 D 项制动到 `pos≈-0.67 cm` 后需等待积分从 `i=-0.47` 累加到约 `-3.19` 才再次移动，形成分段平台。现保留用户当前 `INTEGRAL_ERR_PX=110` 作为积分误差权重完全生效点，新增 `INTEGRAL_START_PX=160`；`110~160 px` 内按 `(160-|err|)/(160-110)` 平滑预积分，`|err|>=160 px` 清零，`|err|<=110 px` 仍沿用原 `|derr|<4` 的低速全量积分，因此原本表现较好的 `0 -> +5 cm` 短行程入口条件不变。按旧反向日志估算，到 `|err|≈110 px` 时积分项可预加载至约 `-1.13`，避免在门限处从零重新建立。用户并行调整后的当前参数以源码为准（记录时 `Kp=0.034`、`Ki=0.005`、`Kd=0.45`、`STOP_ERR_PX=10`），本次未覆盖。`git diff --check` 通过，未执行 Keil 编译、烧录或实车验证。
